---
const items = [
  { num: "+50", label: "indépendants accompagnés" },
  { num: "48h", label: "livraison garantie" },
  { num: "490 €", label: "message + site clé en main" },
  { num: "0", label: "mauvais client toléré" },
];
---

<section class="social-proof">
  <div class="container">
    <div class="proof-grid">
      {items.map(item => (
        <div class="proof-item">
          <span class="proof-num">{item.num}</span>
          <span class="proof-label">{item.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
.social-proof {
  padding: 2.5rem 0;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  background: var(--white);
}

.proof-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
  text-align: center;
}

.proof-item {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.proof-num {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 800;
  color: var(--accent);
  letter-spacing: -0.02em;
}

.proof-label {
  font-size: 0.82rem;
  color: var(--text-muted);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

@media (max-width: 640px) {
  .proof-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem 1rem;
  }
  .proof-num { font-size: 1.3rem; }
}
</style>
